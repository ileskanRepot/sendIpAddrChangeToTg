# sendIpAddrChangeToTg

## How to use

First generate venv `python venv botenv`

Second start venv `source botenv/bin/activate`

Third install requirements `pip install -r requirements.txt`

Fourth generate `.evn` from the `.env.example`

Fifth run the app `python app.py`
